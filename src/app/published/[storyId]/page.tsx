import React from "react";

const PublishedStoryDetailsPage = ({
  params,
}: {
  params: {
    storyId: string;
  };
}) => {
  return <div>
    helo
  </div>;
};

export default PublishedStoryDetailsPage;
